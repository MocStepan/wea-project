server:
  port: 80

spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:postgresql://postgres/postgres
    username: postgres
    password: postgres
auth:
  cors:
    frontendUrl: "localhost:4200"
  jwt:
    access:
      secret: ""
    refresh:
      secret: ""
synchronization:
  book:
    import:
      enabled: true
